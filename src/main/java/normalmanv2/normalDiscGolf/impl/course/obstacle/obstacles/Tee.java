package normalmanv2.normalDiscGolf.impl.course.obstacle.obstacles;

import normalmanv2.normalDiscGolf.impl.course.obstacle.ObstacleImpl;
import org.bukkit.Location;
import org.jetbrains.annotations.Nullable;

public class Tee extends ObstacleImpl {
    public Tee(@Nullable Location location, String schematicName) {
        super(location, schematicName);
    }
}
