package normalmanv2.normalDiscGolf.impl.skill;

import normalmanv2.normalDiscGolf.api.skill.Skill;

public class Flex extends Skill {

    public Flex(int level, int maxLevel) {
        super(level, maxLevel);
    }
}
